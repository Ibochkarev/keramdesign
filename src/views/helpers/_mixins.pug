mixin header-section(data)
  header.section-heading
    .section-heading__wrapper
      - if (data.tag == 'h1')
        h1.section-heading__title!=data.title
      - else
        h2.section-heading__title!=data.title
      if data.link
        a.section-heading__link(href!=data.link aria-label!=data.name)
          span!=data.name
    if data.description
      .section-heading__description!=data.description

//-  Пример использования
//- +header-section({
//-   title: "Полезные статьи",
//-   name: "Все статьи",
//-   tag: "h1",
//-   link: "page-articles.html",
//-   description: ""
//- })

mixin collection-item(data)
  .collection-item
    .collection-item__wrapper
      a(href!=data.link aria-label!=data.title)
        picture
          source(srcset="img/new-collections/" + data.img + ".webp" type="image/webp")
          source(srcset="img/new-collections/" + data.img + ".png" type="image/png")
          img.collection-item__image(src="img/new-collections/" + data.img + ".jpg" alt!=data.title)
      if data.label
        .collection-item__label!=data.label
    .collection-item__headline
      h3.collection-item__title!=data.title
      .collection-item__amount!=data.amount
    .collection-item__info
      .collection-item__brand!=data.brand
      .collection-item__country!=data.country

mixin collection-item-full(data)
  .collection-item.collection-item--full
    .collection-item__wrapper
      a(href!=data.link aria-label!=data.title)
        picture
          source(srcset="img/new-collections/" + data.img + ".webp" type="image/webp")
          source(srcset="img/new-collections/" + data.img + ".png" type="image/png")
          img.collection-item__image(src="img/new-collections/" + data.img + ".jpg" alt!=data.title)
      if data.label
        .collection-item__label!=data.label
    .collection-item__wrap
      .collection-item__info
        .collection-item__headline
          h3.collection-item__title!=data.title
          .collection-item__amount!=data.amount
        .collection-item__description!=data.description
        .collection-item__info
          .collection-item__brand!=data.brand
          .collection-item__country!=data.country
      .collection-item__price-block
        span от
        |
        | 12180 ₽
        |
        | /м
        sup 2

mixin article-card(data)
  .article-item
    a.article-item__info(href="article-page.html")
      picture
        source(srcset="img/articles/" + data.img + ".webp" type="image/webp")
        source(srcset="img/articles/" + data.img + ".png" type="image/png")
        img.article-item__img(src="img/articles/" + data.img + ".jpg" alt!=data.title)
    .article-item__wrap
      .article-item__title!=data.title

mixin svgMono(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class="ico ico-mono-" + name + modClass)
    use(xlink:href= 'img/sprite-mono.svg#ico-mono-' + name)

mixin svgColor(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class="ico ico-color-" + name + modClass)
    use(xlink:href= 'img/sprite-color.svg#ico-color-' + name)

mixin webp(name, typeImg, lazy, alt)
  if lazy == undefined
    picture
      source(srcset="/img/"+name+".webp" type="image/webp")
      source(srcset="/img/"+name+"."+typeImg+"" type="image/"+typeImg+"")
      img(src="/img/"+name+"."+typeImg+"" alt=""+alt+"")
  else
    picture
      source(data-srcset="/img/"+name+".webp" type="image/webp")
      source(data-srcset="/img/"+name+"."+typeImg+"" type="image/"+typeImg+"")
      img(data-src="/img/"+name+"."+typeImg+"" alt=""+alt+"" class=lazy)